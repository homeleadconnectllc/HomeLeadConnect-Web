# Leads — Dashboard Version A alignment

Production-derived presentation-only refinement for `/leads`.

- Keeps existing CRM behavior and data flow unchanged.
- Replaces remaining dark navy application styling with the authenticated Dashboard Version A visual authority.
- Uses light neutral surfaces, HomeLead Connect blue accents, compact KPI summary, tighter search/form controls, denser pipeline rows, and mobile safe-area clearance.
- Preserves centered display hierarchy while keeping operational text, form fields, and row data conventionally aligned for usability.
- No route, auth, permission, billing, AI, workflow, or business-logic changes.
