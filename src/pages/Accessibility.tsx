import { Link } from "react-router-dom";
import PublicSiteNav from "../components/PublicSiteNav";
import { pageImage } from "../config/publicPageImagery";
import "../styles/public-utility-flat.css";
import "../styles/public-board-pages-20260912.css";
const commitments=[["Navigation","Keyboard-operable by design","Navigation and actions should remain reachable without requiring a pointer or touch-only interaction."],["Responsive","No required horizontal scrolling","Public, portal, and workspace layouts are designed to adapt across phone, tablet, desktop, zoom, and safe-area constraints."],["Communication","Status and error information","Forms use semantic labels and visible status or error messaging so users can understand what happened and what to do next."],["Media","Meaningful alternatives","Meaningful imagery and agent portraits should include text alternatives, with captions or transcripts for future instructional media."],["Motion + contrast","Readable and controllable","HomeLead Connect supports reduced-motion-aware presentation and clear contrast targets across high-attention actions and status surfaces."],["States","Clear when something changes","Loading, empty, denied and failure states should be explicit rather than leaving users to infer whether an action succeeded."]] as const;
export default function Accessibility(){const visual=pageImage("accessibility");return <main className="hlc-utility-page hlc-public-board-page" data-public-page="accessibility"><PublicSiteNav/><div className="hlc-utility-shell hlc-public-shell hlc-public-shell--visual"><header className="hlc-utility-header hlc-public-hero"><div><p className="hlc-utility-kicker hlc-public-kicker">Accessibility</p><h1 className="hlc-utility-title">HomeLead Connect should work across devices, inputs, and abilities.</h1><p className="hlc-utility-lead hlc-public-intro-copy">HomeLead Connect is being built for keyboard, screen-reader, zoom, contrast, reduced-motion and phone use across public, portal and workspace experiences.</p><div className="hlc-utility-actions"><Link className="hlc-utility-primary hlc-public-primary" to="/contact">Report an accessibility problem</Link><Link className="hlc-utility-secondary hlc-public-secondary" to="/">Back to HomeLead Connect</Link></div></div></header><figure className="hlc-public-visual"><img src={visual.src} alt={visual.alt} loading="eager"/></figure><section className="hlc-board-access-grid" aria-label="Accessibility commitments">{commitments.map(([label,title,body])=><article className="hlc-board-access-card" key={title}><p>{label}</p><h2>{title}</h2><span>{body}</span></article>)}</section><section className="hlc-utility-focus hlc-public-offer"><p className="hlc-utility-section-label hlc-public-offer-label">Need help?</p><h2>Report a problem with enough context to reproduce it.</h2><p>Include the page, device, browser and the action you could not complete. Do not send passwords, authentication codes or sensitive job information through a general support message.</p><div className="hlc-utility-actions"><Link className="hlc-utility-primary hlc-public-primary" to="/contact">Contact HomeLead Connect</Link></div></section><section className="hlc-board-access-grid"><article className="hlc-board-access-card"><p>Release standard</p><h2>Accessibility acceptance is part of deployment readiness.</h2><span>Formal WCAG acceptance testing is still required on each deployment candidate. A polished interface is not treated as proof of accessibility by itself.</span></article></section></div></main>}
